// const players = ["Kyler", "Hopkins", "Green"];

// const offenseIntro = players.map( (element) => {return `${element} plays for the Arizona Cardinals.`} );

// console.log(offenseIntro);

function divide() {

   return 2000 / 100;
}

const square = (num1) => num1 * num1;

const add = (num2, num3) => {return num2 + num3;}